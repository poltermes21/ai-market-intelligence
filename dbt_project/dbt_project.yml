name: 'market_intelligence'
version: '1.0.0'
config-version: 2
profile: 'market_intelligence'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  market_intelligence:
    staging:
      materialized: view
      schema: staging
    marts:
      materialized: table
      schema: marts
